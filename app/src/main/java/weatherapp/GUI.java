package weatherapp;

import javax.swing.JFrame;

public class GUI extends JFrame {

    public GUI() {
        setSize(500, 500);
        setName("Weather App");
        setTitle(getName());
    }

}
